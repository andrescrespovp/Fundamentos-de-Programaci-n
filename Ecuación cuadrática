#include <iostream>
#include <math.h>
using namespace std;

int main ()
{
	float a, b, c, x1, x2;
	cout<<"Ingresa el valor de a: "<<endl;
	cin>> a;
	cout<<"Ingresa el valor de b: "<<endl;
	cin>> b;
	cout<<"Ingresa el valor de c: "<<endl;
	cin>> c;
	float d= b * b - (4 * a * c);
if (a==0)
	cout<<endl<<"La ecuación no es cuadrática";
else{
	if( d>0 ){
		x1 = (-b + sqrt (b * b - (4 * a * c)))/(2 * a);
		x2 = (-b - sqrt (b * b - (4 * a * c)))/(2 * a);
		cout<<"Raíz x1: "<< x1<<endl;
		cout<<"Raíz x2: "<< x2;
		  }else
			if (d<0)
			cout<<"La ecuación no tiene soluciones reales.";
				else
				     if (d==0){
					      x1 = -b + sqrt (b * b - (4 * a * c));
					      cout<<"La única solución real es: "<<x1;
					      }
	     }
return 0;
     }

