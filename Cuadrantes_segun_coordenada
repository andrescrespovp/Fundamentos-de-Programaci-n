#include <iostream>
using namespace std;
int main()
{
    float x, y;
    char separador;
    cout<<"Escribe valor de x del punto"<<endl;
    cin>> x;
    cout<<"Escribe valor de y del punto"<<endl;
    cin>> y;
    if(x>0 && y>0)
        cout<<"El punto está en el primer cuadrante.";
        else if(x<0 && y>0)
            cout<<"El punto está en el segundo cuadrante.";
            else if (x<0 && y<0)
                cout<<"El punto está en el tercer cuadrante.";
                else if (x>0 && y<0)
                    cout<< "El punto está en el cuarto cuadrante.";
    if(x==0 && y!=0)
        cout<<"El punto está sobre el eje x";
        else if(y==0)
            cout<<"El punto está sobre el eje y";
            else if(x==0 && y==0)
                cout<<"El punto es el orígen.";
return 0;
}
